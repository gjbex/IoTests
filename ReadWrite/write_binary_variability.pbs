#!/bin/bash -l
#PBS -l nodes=1:ppn=20
#PBS -l mem=60gb
#PBS -l walltime=00:30:00

cd $PBS_O_WORKDIR

size=$(( 1024**3 ))
buffer_size=4096

for iter in `seq 5`
do
    ./writer -buffer ${buffer_size} -size ${size} \
             -mode binary -file ${VSC_SCRATCH}/data-${iter}.bin
done
